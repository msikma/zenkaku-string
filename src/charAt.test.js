// © 2019, MIT license

const { wideCharAt } = require('./charAt')

describe('Zenkaku-string', () => {
  describe('wideCharAt()', () => {
    describe('character at given position is returned when', () => {
      it('is a regular character', () => {
        expect(0).toBe(0)
      })
    })
  })
})
